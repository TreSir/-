# 优化任务台账

> 本文档用于记录可选的工程优化建议、预期收益和决策状态。**列入台账不代表已批准，也不代表会自动执行。** 只有项目负责人明确批准的任务才进入实现；待决策任务仅作记录。

## 如何维护（增删查改）

- **新增**：在“任务列表”增加一行，分配不重复的 `OPT-编号`，并在“任务详情”补上收益、成本、风险、依据、架构影响和验收标准。
- **查询**：按 ID、状态、建议优先级或领域筛选任务列表；详情中的“依据”提供对应文件和当前观察。
- **修改**：保留 ID，更新任务字段和详情；状态或方案发生变化时，在“决策记录”写明日期与原因。
- **删除**：可直接删除任务行及对应详情。若希望保留审计轨迹，可改为“已拒绝”或“已归档”，并记录原因。
- **执行**：负责人明确批准后，才将状态改为“已批准”。实现者不得仅因任务被记录或标为建议优先级就开始改代码。

## 架构保护与决策规则

> **任何优化都不得破坏游戏整体架构或绕过既有边界。列入台账、评估出收益或标记优先级，都不代表获准调整架构。**

- 架构设计背景与原则参阅[《整体架构与玩法逻辑设计》](Godot_悬疑侦查游戏_架构与死亡笔记玩法设计.md)。该文档标注为设计参考；当前实现以仓库根目录 `README.md` 和实际代码为准，若不一致，先核对现状，不照搬文档中的旧路径或旧示例。
- 每个新增任务都要说明是否触及架构边界，以及能否在保持当前架构的前提下完成。任何实现都必须维持当前职责边界、状态写入规则、数据加载路径和小游戏接口契约。
- 如果认为调整架构能带来更好的**游戏性能、美术表现/资源流程或架构可维护性**，必须先向负责人报告：当前限制、为何需要调整、保持现架构的替代方案、预期收益与依据、成本和风险、影响范围及验证办法。没有明确决策前，任务保持“待决策”，不得擅自改代码、数据格式或资源管线。
- 负责人批准某项功能，不自动等于批准其架构调整。若实施中发现必须改变架构，应暂停相关改动、补充方案并等待负责人决定；决定和批准范围记入“决策记录”。

### 状态定义

| 状态 | 含义 |
| --- | --- |
| 待决策 | 仅是候选项；尚未授权实施 |
| 已批准 | 负责人明确同意，可安排实施 |
| 执行中 | 已开始实施 |
| 已完成 | 已实现并达到验收标准 |
| 已拒绝 | 已决定不做，保留决策记录 |
| 已归档 | 暂不处理或被其他任务替代 |

### 估算口径

- 收益：高 / 中 / 低，描述预期改善，不代表保证结果。
- 成本：低（局部小改）/ 中（需调整多个模块或补验证）/ 高（跨层改造或迁移方案）。
- 建议优先级只是供负责人排序的参考，最终由负责人决定是否做、何时做。

## 任务列表

| ID | 领域 | 候选优化 | 收益 | 成本 | 架构影响（需评估） | 建议优先级 | 状态 | 决策 / 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| OPT-001 | UI / 可维护性 | 逐步拆分 `main.gd` 的页面与界面职责 | 高 | 中 | 表现层内拆出案件面板；状态写入边界未变 | 高 | 已完成 | 案件页由 `case_panel.gd` 绘制；其余页面可按需逐步拆分 |
| OPT-002 | 存档 / 兼容性 | 为存档增加版本迁移与失败备份恢复 | 高 | 中 | 存档模块内扩展；调查门面仍是状态提交入口 | 高（外测前） | 已完成 | 同内容标识的 schema 1→2；普通存档可从有效备份恢复 |
| OPT-003 | UI / 更新效率 | 将通用全量刷新收窄为按区域更新 | 中 | 中 | 可能；需保持表现层只消费状态 | 中 | 待决策 | 当前规模未显示性能问题，可等实测或页面增长后再评估 |
| OPT-004 | 状态 / 效率 | 检视 GameState 事务中的重复复制与校验 | 低至中 | 低至中 | 是；不得削弱事务原子性和校验边界 | 低 | 待决策 | 先以性能测量确认是否值得调整 |
| OPT-005 | 剧情数据 / 整洁度 | 确认 `prologue.package_received` 是否仍有用途 | 低 | 低 | 否；限于剧情数据检查，不改变系统边界 | 低 | 待决策 | 若为预留剧情钩子则注明用途；无用途再考虑移除 |
| OPT-006 | 表现层 / 状态一致性 | 「回顾」跟着世界一起作废（新开一局 / 读档 / 回溯） | 中 | 低 | 否；只加一条生命周期信号，状态写入与存档边界未变 | 中 | 已完成 | 调查门面发 `world_replaced`，`main` 清空 `_history` |
| OPT-007 | 数据契约 | 给 `transitions.json` 补加载期校验 | 中 | 低 | 否；`kind` 词表并入既有「校验与执行同一张表」规矩 | 中 | 已完成 | 分组 / 引用 / 字段 / 时长 / 颜色四类坏数据在编译期被拒 |

## 任务详情

### OPT-001 — 逐步拆分主界面脚本

- **现状 / 依据**：`scripts/black_page/main.gd` 同时包含界面构建、页面切换、输入处理和剧情展示等职责，后续页面增加时修改范围会扩大。
- **预期收益**：降低页面之间的耦合，让 UI 改动更容易定位、审查和测试。
- **成本 / 影响**：中。需要渐进迁移并验证既有流程；一次性重写会带来不必要的回归风险。
- **不做的代价**：脚本继续增长后，职责边界和回归定位可能更困难；当前尚无立即故障。
- **建议验收**：至少一个高内聚页面职责迁至独立场景或控制器；原有输入、导航、剧情流程通过验证；不改变现有游戏状态写入边界。
- **执行边界**：获批后按小步拆分，不默认授权全面重构。
- **完成记录（2026-09-23）**：将案件页绘制、进展注脚和局部控件移入 `scripts/black_page/case_panel.gd`；`main.gd` 保留导航、弹层生命周期和调查回调。冒烟测试覆盖案件页打开与调查流程；没有改变数据层或 GameState 写入规则。

### OPT-002 — 存档迁移与失败恢复

- **现状 / 依据**：`scripts/core/save_manager.gd` 当前有存档 schema 版本；版本不匹配会拒绝加载，尚无迁移链。垂直切片阶段可以接受。
- **预期收益**：剧情数据和存档结构演进时，降低旧存档无法继续使用的风险。
- **成本 / 影响**：中。需要定义迁移策略、旧格式测试样例，以及迁移失败时的恢复行为。
- **不做的代价**：未来改动存档结构或内容标识后，玩家旧档可能不能直接载入。
- **建议验收**：至少支持从上一个存档版本迁移到当前版本；迁移失败不覆盖原存档；有自动化测试覆盖成功、未知版本和损坏存档。
- **建议时机**：面向外部测试者发放存档后，或首次进行破坏兼容性的存档变更前。
- **完成记录（2026-09-23）**：`SaveManager` 增加显式 schema 1→2 迁移并在内存中完成验证；`SaveStore` 写入前备份可读取的旧存档，普通存档可从通过校验的 `.json.bak` 恢复。检查点不使用旧备份。测试覆盖迁移、未知版本、损坏主档、备份恢复和失败不覆盖原档；不同内容标识不自动迁移。

### OPT-003 — 按变更域刷新 UI

- **现状 / 依据**：`scripts/black_page/main.gd` 中通用 `refresh()` 会更新多个 HUD / 导航相关 UI；随着状态变化和页面增加，可能出现不必要的刷新。
- **预期收益**：让状态变化与 UI 更新范围更清晰，减少不相关控件重复更新。
- **成本 / 影响**：中。需要明确刷新域或变更事件；拆分过细也会提高维护复杂度。
- **不做的代价**：主要是将来扩展时的更新耦合；当前没有确认的卡顿或性能瓶颈。
- **建议验收**：局部状态变化只刷新相关区域；完整页面切换仍正确；必要时记录刷新次数或用性能工具验证收益。
- **建议时机**：出现可复现的刷新问题、性能数据支持，或 UI 页面数量显著增长时。

### OPT-004 — 评估状态事务的复制与校验次数

- **现状 / 依据**：`scripts/services/game_state.gd` 的效果事务会准备并验证候选状态，再经恢复流程进行校验 / 复制；当前状态规模很小，稳健性优先。
- **预期收益**：若状态数据显著增长并被证明是热点，可减少不必要的复制与重复校验。
- **成本 / 影响**：低至中。内部提交流程若改错，可能削弱事务原子性或校验完整性。
- **不做的代价**：现阶段预计影响很小；不应为理论上的性能收益牺牲健壮性。
- **建议验收**：先有可复现的基准数据；调整后事务失败仍不污染原状态；相关类型和范围校验保持完整；基准结果有改善。
- **建议时机**：只有性能测量显示该路径值得优化时。

### OPT-005 — 确认未使用的剧情 Flag

- **现状 / 依据**：架构审计提示 `data/black_page/flags.json` 中的 `prologue.package_received` 有写入但未发现读取。
- **预期收益**：清除无效数据或补充设计说明，减少策划对 Flag 实际效果的误解。
- **成本 / 影响**：低。若它是未来分支预留，过早删除会丢失约定。
- **不做的代价**：保留未使用项会带来轻微的数据噪声，不影响当前运行。
- **建议验收**：确认剧情设计用途；若确实无用途再删除，并通过数据审计；若保留则在 Flag 描述或剧情文档中注明预期用途。

### OPT-006 — 「回顾」跟着世界一起作废

- **现状 / 依据**：`scripts/black_page/main.gd` 的 `_history` 是「剧情回顾」面板的唯一数据源，只有写入没有任何清空；而 `investigation.gd` 的 `new_game()` 与 `_restore_validated()`（读档、断链回溯共用这一条）都会重置引擎侧的 `journal`。于是游戏内「重新开始」之后，回顾仍能翻到上一局的正文。
- **预期收益**：消除跨世界线漏字；让「玩家看过的文字」与世界的生命周期一致。
- **成本 / 影响**：低。在调查门面上加一条生命周期信号，表现层监听后清空；不改状态写入规则、不改存档格式、不改数据。
- **不做的代价**：回溯 / 读档 / 重开后，界面呈现不属于当前世界线的剧情文字，玩家会当成剧情错乱。
- **建议验收**：冒烟断言「记录一句 → 走还原路径 → 回顾为空」，且既有回顾断言不受影响。
- **完成记录（2026-09-23）**：`investigation.gd` 新增 `world_replaced` 信号，在 `new_game()` 与 `_restore_validated()` 两处发出；`main.gd` 新增 `_on_world_replaced()` 清空 `_history`，在 `_ready()` 里与 `changed` 一同连接。冒烟走 `ui.game.restore(ui.game.snapshot())`（与读档 / 回溯同一条还原路径，又不真的换掉世界，后面的界面断言照常跑）新增 3 条断言。检查数 290 → 297，`UI_CHECK_FLOOR` 同步到 296。

### OPT-007 — `transitions.json` 的加载期校验

- **现状 / 依据**：`transitions.json` 在 `data_loader.gd` 的加载列表里，却是 12 个数据文件中唯一**一个字都不校验**的一张表。`main._transition_config()` 按「行动 > 场景 > 默认」三层合并，所以分组拼成 `scene`、场景或行动 id 写错、`kind` 拼成 `"fadee"`，全都静默退回默认淡入淡出——配置改了却没生效，运行时也不报错。
- **预期收益**：把转场配置错误从「运行时静默无效果」提前到加载期，并带上文件与行号。
- **成本 / 影响**：低。只新增校验；转场 `kind` 词表收进 `scenes.gd.KINDS`，由校验与执行共同读取（沿用 `Runner.COMMANDS` / `Director.ACTIONS` / `MiniGameResult.TYPES` 的既有规矩）。
- **不做的代价**：内容作者改转场得不到任何反馈，错配会长期潜伏。
- **建议验收**：未知分组、未知场景 / 行动 id、未知 `kind`、非法时长四类坏数据在编译期被拒；现有数据照常通过。
- **完成记录（2026-09-23）**：`data_loader.gd` 新增 `_compile_transitions()` 与 `_validate_transition()`（分组白名单、`scenes` / `actions` 的键必须是真实场景与行动、字段白名单、`kind` 查 `Scenes.KINDS`、时长为正数秒、颜色为 6 或 8 位十六进制）；`scenes.gd` 增 `KIND_FADE / KIND_CUT / KIND_SLIDE / KINDS`，`main._transition_to()` 与 `_transition_config()` 改读这张表。冒烟新增 4 条坏数据断言。

## 决策记录

| 日期 | 任务 ID | 决策 | 原因 / 范围 |
| --- | --- | --- | --- |
| 2026-09-23 | OPT-001–OPT-005 | 建立候选台账，全部保持待决策 | 记录巡检发现；未授权实施任何优化 |
| 2026-09-23 | OPT-001、OPT-002 | 用户批准并完成 | 案件页渐进拆分、同内容标识存档迁移与备份恢复；保留原架构边界，验证见任务详情 |
| 2026-09-23 | OPT-006、OPT-007 | 用户批准并完成 | 架构评审复核后确认的两项真问题：回顾随世界作废、转场配置补校验。都不触及状态写入边界、存档格式与数据形状 |

## 后续记录约定

任务完成后补充完成日期、改动范围、验证方式与结果；若任务被拆分，建立新的子任务 ID，并在原条目中互相引用。优先级、收益和成本允许随项目情况更新，但应在“决策记录”说明影响决策的变化。
